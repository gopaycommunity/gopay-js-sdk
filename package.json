{
    "name": "gp-gw-js-sdk-root",
    "private": true,
    "packageManager": "yarn@4.13.0",
    "workspaces": [
        "sdk",
        "example"
    ],
    "scripts": {
        "lint": "biome check .",
        "ci": "yarn biome ci . && yarn workspace gopay-js-sdk run typecheck && yarn workspace gopay-js-sdk run check:circular && yarn workspace gopay-js-sdk run check:exports",
        "prepare": "husky",
        "test:e2e": "playwright test",
        "example": "yarn workspace gopay-js-sdk run build && yarn workspace gopay-js-sdk-example run dev"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.4.11",
        "@commitlint/cli": "^20.5.0",
        "@commitlint/config-conventional": "^20.5.0",
        "@playwright/test": "^1.59.1",
        "husky": "^9.1.7"
    },
    "commitlint": {
        "extends": [
            "@commitlint/config-conventional"
        ],
        "parserPreset": {
            "parserOpts": {
                "issuePrefixes": [
                    "GPOMA-"
                ]
            }
        },
        "rules": {
            "references-empty": [
                2,
                "never"
            ]
        }
    }
}
