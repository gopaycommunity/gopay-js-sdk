# Disable npm workspace mode so that `npm version` (used by @semantic-release/npm)
# does not try to resolve Yarn-specific `workspace:*` protocol references
# found in example/package.json.
# Yarn does not read .npmrc, so this does not affect the Yarn-based dev workflow.
workspaces=false
